# Ping-Pioneers_A_P2-P3
This project is a Network Traffic Analyzer developed in C++ using the libpcap library. It captures and analyzes live network packets from a selected network interface. The program reads each packet, extracts the source and destination IP addresses, identifies the protocol type (TCP, UDP, ICMP, or others), and maintains a count of each protocol.
